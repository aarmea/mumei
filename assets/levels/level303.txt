  moveX(int dist): is a function which 
                   moves robot right a 
                   distance specified 
                   by an integer value

  int adjacentTo: integer value which is one
                  when adjacent to a skeleton
 
  punch(): knock a skeleton out of the way
           only works when adjacent
  